<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>VALORANT — Login</title>
  <link href="https://fonts.googleapis.com/css2?family=DIN+Next+LT+Pro:wght@400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="login-body">
  <canvas id="bg-canvas"></canvas>

  <div class="login-container">
    <div class="login-logo">
      <img src="/assets/extra/ValorantLogo.png" alt="Valorant" class="login-logo-img">
      <div class="login-logo-text">VALORANT</div>
      <div class="login-logo-sub">Agent Intelligence System</div>
    </div>

    <div class="login-card">
      <div class="login-card-header">
        <div class="login-title">LOGIN</div>
        <div class="login-corner"></div>
      </div>

      <% if (typeof error !== 'undefined' && error) { %>
        <div class="login-error"><%= error %></div>
      <% } %>

      <form class="login-form" method="POST" action="/login">
        <div class="login-field">
          <label class="login-label">Username</label>
          <input class="login-input" type="text" name="username" placeholder="Enter username" autocomplete="off" required>
        </div>
        <div class="login-field">
          <label class="login-label">Password</label>
          <input class="login-input" type="password" name="password" placeholder="Enter password" required>
        </div>
        <button class="login-btn" type="submit">LOGIN</button>
      </form>

      <div class="login-footer">
        No account? <a href="/register" class="login-link">Register here</a>
      </div>
    </div>
  </div>

  <script src="/js/canvas.js"></script>
</body>
</html>
